import type { Metadata } from "next";
import { Inter, Geist_Mono } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "@/components/theme/theme-provider";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Ventiy - Your Safe Space for Emotional Wellness",
  description: "Anonymous venting, AI-driven insights, and pathways to professional care. Join Ventiy for a supportive mental wellness journey.",
  keywords: ["mental wellness", "emotional support", "anonymous venting", "therapy", "AI insights", "emotional health"],
  openGraph: {
    title: "Ventiy - Your Safe Space for Emotional Wellness",
    description: "Anonymous venting, AI-driven insights, and pathways to professional care.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} ${geistMono.variable} antialiased`}
      >
         <ThemeProvider
            attribute="class"
            defaultTheme="system"
            enableSystem
            disableTransitionOnChange
          >
        {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
